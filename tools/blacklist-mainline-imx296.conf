# NVIDIA tegracam nv_imx296 is required for IMX296LL on JetPack 6.2.
# The mainline imx296 driver shares the same OF aliases (sony,imx296),
# probes first, fails (no sensor clock), then blocks nv_imx296 from binding.
blacklist imx296
install imx296 /sbin/modprobe --ignore-install nv_imx296
